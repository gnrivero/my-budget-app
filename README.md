# My Budget in React Native

## Getting Started
To run the app, simply run

``` npm install react-native ```
``` npm install node ```
``` npm install && expo start ```

And scan the QR code in the Expo client app.
